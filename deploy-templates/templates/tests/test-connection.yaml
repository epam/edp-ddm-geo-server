apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geo-server.fullname" . }}-test-connection"
  labels:
    {{- include "geo-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "geo-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
